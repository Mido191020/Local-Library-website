extends layout

block content
  h1= title
  if book_list.length
    ul
      each book in book_list
        li
          a(href=`/catalog/books/${book._id}`) #{book.title}
          |  (#{book.author.name})

  else
    p There are no books.